# Locations

import Locations from '@site/src/components/Locations';

Here's a list of all items that can be used in the **/location** command. In order to find it, just type (part of) the name of the location you see below. Depending on the language, you may need to
type in the equivalent name in the language of your server.

<Locations />
