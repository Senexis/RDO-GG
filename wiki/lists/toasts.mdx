# Toasts

import Toasts from '@site/src/components/Toasts';

Here's a list of all items that can be used as icon in the **/toast** command. In order to find it, just type (part of) the name of the toast you see below. Depending on the language, you may need to
type in the equivalent name in the language of your server.

<Toasts />
