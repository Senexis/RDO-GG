# Locations

import Locations from '@site/src/components/Locations';

Here's a list of all items that can be used in the **/location** command. To find a location, type (part of) its name below. Depending on the language, you may need to type the equivalent name in the language of your server.

<Locations />
